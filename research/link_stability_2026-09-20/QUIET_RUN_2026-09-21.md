# Quiet 24 h run — 2026-09-21 → 2026-09-22

**Question: why does the `IR2UFV ↔ IW2OHX-12` link keep recycling?**
v2.2.1 fixed three real defects in the `3+` full-table exchange and the
teardowns continued at the same rate, so the mechanism is still unknown.
See `research/link_stability_2026-09-20/V2_2_1_OUTCOME.md`.

- **Start:** 2026-09-21T10:07Z (captures), 10:15:08Z (run controller)
- **Deadline:** 2026-09-22T10:15:08Z — the controller stops the captures,
  restores the crons and writes `/tmp/quiet-run/DONE`
- **Controller:** `/tmp/quiet-run-24h.sh`, log `/tmp/quiet-run/run.log`

## What makes this run different: nothing is polling the nodes

This is the point of the run. From this project's own history — *"the
monitoring caused most of what it measured"* — and it was worse than that
note implies:

| Poller | Was doing | Now |
|---|---|---|
| `station-dashboard` cron, gw | telnet **all five** nodes every **15 min**, including a chained hop to `-12` **across the live mesh** | **disabled** |
| `xnet-web-status` cron, gw | telnet `IW2OHX-14` every **10 min** | **disabled** |
| `iw2ohx-monitor`, macmini LaunchAgent | SSH-probe every host every **30 min**; can telnet `IW2OHX-4` and re-upload its port config | **unloaded** |
| `fl-sampler.sh`, gw | telnet production every **5 min** | **killed** |

**PC/Flexnet's `3+` interval is only 75-90 min.** A poll every 15 minutes
was landing inside every window we have ever measured. Any previous
stability rate for this link was measured with that in it.

`ping-iw2ohx2.sh` (*/5) is left running — it pings the NAS, not a FlexNet
node.

### Restoring, if it has not happened automatically
```
bash /tmp/quiet-run/restore-pollers.sh          # gw crons
bash ~/.config/quiet-run/restore-monitor.sh     # macmini monitor agent
```
The macmini side also has a one-shot LaunchAgent,
`net.iw2ohx.quiet-run-restore`, firing 12:30 CEST on 2026-09-22, which
unloads itself after it runs.

## What is recording

| Path | Link | Filter |
|---|---|---|
| `/tmp/rtr-mon-12/link.pcap*` | **IR2UFV ↔ IW2OHX-12 — the question** | `udp port 10075 and not port 10093 and host 192.168.1.201` |
| `/tmp/rtr-mon-14/link.pcap*` | IR2UFV ↔ IW2OHX-14 | `… host 44.134.24.4` |
| `/tmp/rtr-mon-4/link.pcap*` | IR2UFV ↔ IW2OHX-4 | `… host 192.168.1.203` |
| `/tmp/prod-router-watch/link.pcap*` | production IW2OHX-13, all peers | `udp port 10093` |

All `-W 8 -C 10` (8 × 10 MB rotating). The `-12` link produced 3.7 MB in
27.5 h previously, so 24 h will not come close to rotating.

**`/tmp` is tmpfs — RAM.** The controller rsyncs every pcap hourly to
`/home/iw2ohx/flexnet-quiet-run-2026-09-21/` and again at the end, because
the station runs on a UPS with no signalling port and a power event would
otherwise take the whole run with it.

## ⚠ Read before comparing against anything older

Two topology changes landed *just* before this run, so **do not compare it
with the `tfix-mon-*` captures** (which end 2026-09-21T09:39:03Z):

| When (UTC) | Change |
|---|---|
| 09:32Z | production `IW2OHX-13` promoted leaf → **FlexNet router** |
| 10:01Z | the **`IR2UFV ↔ IW2OHX-4` peering re-enabled** |

IR2UFV now has three peers instead of two, and `IW2OHX-4` now routes
almost everything through the two LinBPQ nodes (`-12` went from 138 chosen
destinations to **0**). Advertisement volume, `learned[]` size and the cost
landscape all moved for reasons unrelated to the `3+` answer.

## Analysis recipe

On `iw2ohx-gw`, against `/tmp/rtr-mon-12/link.pcap0`:

```bash
# every '3+' full-table request -> what we answered -> the next teardown
python3 /tmp/plus.py /tmp/rtr-mon-12/link.pcap0 192.168.1.202 192.168.1.201

# who tore the link down, pinned (NEVER omit --local-ip or --link-only)
cd /home/iw2ohx/xnet_investigation_agent/linbpq-flexnet/tools
python3 axudp_teardown.py --local-ip 192.168.1.202 --peer-ip 192.168.1.201 \
        --link-only /tmp/rtr-mon-12/link.pcap0

# the frames immediately before each teardown
python3 disc_context.py …
```

**Counting rules that have inverted these statistics before:**
- Teardowns arrive in **pairs 60 s apart**; the second is PC/Flexnet's
  fresh-session seed. Count each pair as **one** event.
- A **quiet hour proves nothing** — confirm a `3+` actually arrived.
- PCF's `600` is its session seed, not a verdict on what preceded it.
- The outbound **link-time frame** and the **advertisement-volume spike**
  both correlate strongly with teardowns and are *part of* the `3+`
  answer, so they carry its timestamps. They are artefacts.

**The one thing never yet tested**, and the reason for this run: what
PC/Flexnet does with a **correct but large** answer — 157-171 records at
16 per frame, one frame per 5 s, spread over 40-120 s. Before v2.2.1 that
shape was never emitted, so no earlier capture contains it.
